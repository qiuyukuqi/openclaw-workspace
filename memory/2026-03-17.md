# 2026-03-17 记忆日志

## 记忆系统故障修复

### 问题
- 用户反馈"我说过的话你老忘记"
- `memory_recall` 返回空结果

### 诊断
1. LanceDB 数据库文件存在（2470个 .lance 文件）
2. 但 `db.tableNames()` 返回空数组 `[]`
3. 表结构丢失，可能是某次重启/升级导致

### 修复
1. 从最新备份 `memory-backup-2026-03-16.jsonl` 恢复（53条记录）
2. 重新存储关键记忆（服务器配置、考试答案路径）

### 防护措施（已部署）
| 脚本 | 频率 | 功能 |
|------|------|------|
| memory-healthcheck.sh | 每小时 | 检测表状态，空表自动恢复 |
| memory-backup.sh | 每6小时 | 创建快照备份 |

### 新增铁律
- **Rule 21 — 记忆数据库健康防护**：已写入 MEMORY.md

---

## 其他事项
- 消防知识考试答题完成
- 有限空间考试答题完成
